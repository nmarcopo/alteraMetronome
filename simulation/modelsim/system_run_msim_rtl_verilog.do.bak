transcript on
if {[file exists rtl_work]} {
	vdel -lib rtl_work -all
}
vlib rtl_work
vmap work rtl_work

vlog -vlog01compat -work work +incdir+N:/Logic\ Lab/Midterm\ Project {N:/Logic Lab/Midterm Project/timer.v}

